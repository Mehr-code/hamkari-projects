import { createContext } from "react";

// Create a React Context for user authentication data
// This will be used to share user info and auth functions across the app
export const UserContext = createContext();
